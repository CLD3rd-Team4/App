spring:
  application:
    name: gateway


jwt:
  secret:
  expiration: 86400000

server:
  port: 8080

# from config server

#spring:
#  application:
#    name: gateway
#  config:
#    import: "configserver:http://config.platform.svc.cluster.local:8888"
#  profiles:
#    active: dev