server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/CLD3rd-Team4/App
          default-label: ECS-55-backend/config
          search-paths: backend/config-repo/{application},backend/config-repo

# 보안 설정 (선택사항)
# security:
#   user:
#     name: configuser
#     password: configpassword
