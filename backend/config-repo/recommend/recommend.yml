# Recommend 서비스 설정

server:
  port: 8082

spring:
  application:
    name: recommend
  datasource:
    url: jdbc:mysql://localhost:3306/mapzip_recommend
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
