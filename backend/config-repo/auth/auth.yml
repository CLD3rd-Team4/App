# Auth 서비스 설정 템플릿
# Auth 팀원이 아래 설정들을 참고해서 직접 작성해주세요

# 서버 포트 설정
# server:
#   port: 8081

# 애플리케이션 기본 설정
# spring:
#   application:
#     name: auth
#   datasource:
#     url: jdbc:mysql://localhost:3306/mapzip_auth
#     username: root
#     password: password
#     driver-class-name: com.mysql.cj.jdbc.Driver
#   jpa:
#     hibernate:
#       ddl-auto: update
#     show-sql: true

# JWT 설정 (예시)
# jwt:
#   secret: mapzip-secret-key
#   expiration: 86400000  # 24시간

# 추가로 고려해야 할 설정들:
# - 실제 데이터베이스 연결 정보
# - JWT 토큰 보안 설정
# - 패스워드 암호화 설정
# - OAuth2 설정 (소셜 로그인)
# - 세션 관리 설정
# - 보안 헤더 설정
