# Schedule 서비스 설정

server:
  port: 8084

spring:
  application:
    name: schedule
  datasource:
    url: jdbc:mysql://localhost:3306/mapzip_schedule
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
